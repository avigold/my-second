# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environment
.venv/
venv/
env/

# Distribution / packaging
dist/
build/

# ── Node / Frontend ───────────────────────────────────────────────────────────
node_modules/
web/novelty-browser/node_modules/

# Vite build output — built on the server by deploy.sh (npm ci && npm run build)
web/static/dist/

# ── Local data & runtime state ────────────────────────────────────────────────
# SQLite databases (populated at runtime — never commit user data)
data/cache.sqlite
data/cache.sqlite-shm
data/cache.sqlite-wal
data/jobs.sqlite
data/uploads/

# PGN output files
data/output/*
!data/output/.gitkeep
*.pgn

# ── OS / tooling ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/
.env
