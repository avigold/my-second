# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environment
.venv/
venv/
env/

# Distribution / packaging (Python build artifacts)
dist/
build/
# But keep the Vite output — server has no Node.js, deploy is git pull only
!web/static/dist/
!web/static/dist/**

# ── Node / Frontend ───────────────────────────────────────────────────────────
node_modules/
web/novelty-browser/node_modules/

# Vite build output is committed so the server (no Node.js) can deploy via git pull.
# Always run `npm run build` in web/novelty-browser/ before committing frontend changes.

# ── Local data & runtime state ────────────────────────────────────────────────
# SQLite databases (populated at runtime — never commit user data)
data/cache.sqlite
data/cache.sqlite-shm
data/cache.sqlite-wal
data/jobs.sqlite
data/uploads/

# PGN output files
data/output/*
!data/output/.gitkeep
*.pgn

# ── OS / tooling ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/
.env
